package com.sophie.sampleapp.domain.interactor

import com.sophie.sampleapp.domain.repository.MainRepository

class GetMain(private val mainRepository: MainRepository) {

    // TODO: This is an example of Use case (interactor between repo and model)
}
